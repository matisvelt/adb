# RigSort Roadmap

## v0.2 (current)
- Offline SSD scan + preview + worker inference + rules + move + audit + undo.

## v0.3 (future)
- Improved rule editor (validation + suggestions)
- Incremental scan (watch mode)
- Better preview formats for HEIC/RAW
