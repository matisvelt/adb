# Security Notes

- Devices are headless; no manual interaction required for runtime.
- ADB access is limited to the host machine running RigControl.
- ADB keys should be managed centrally and kept off shared storage.
- No device data is transmitted outside the host unless explicitly added in future phases.
